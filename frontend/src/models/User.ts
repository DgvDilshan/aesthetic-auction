export type UserProfileToken = {
  userName: string;
  email: string;
  phoneNumber: string;
  token: string;
};

export type UserProfile = {
  userName: string;
  email: string;
  phoneNumber: string;
};
